PROGRAMA
VARIABLES
    i,max,min:NUM;
    s:SEQ(NUM);
    n:SEQ(LOG);
SUBPROGRAMAS
    FUNCION mayor_que_5(NUM x) dev (LOG r)
    VARIABLES
    INSTRUCCIONES
        si (x>5) entonces
            dev T;
        sino
            dev F;
        fsi
    FFUNCION
INSTRUCCIONES
    x = 1;
    y = 2;
    y = y,x+2;
    s = [s,2,5,3,5,0];